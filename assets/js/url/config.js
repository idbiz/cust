import { folderPath } from "https://cdn.jsdelivr.net/gh/jscroot/url@0.0.9/croot.js";

const baseAPIURL = 'https://asia-southeast2-awangga.cloudfunctions.net/idbiz'

export const conf = {
    user: `${baseAPIURL}/data/user`,
}